language: node_js

node_js:
  - "0.10"

addons:
  firefox: "23.0"

services:
  - selenium

before_script:
  - "export DISPLAY=:99.0"
  - "/etc/init.d/xvfb start"
  - mkdir -p shippable/testresults
  - mkdir -p shippable/codecoverage
  
cache: true

archive: true
